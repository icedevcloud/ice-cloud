server:
  port: 5000

spring:
  application:
    name: bing-upms
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  boot:
    admin:
      client:
        url: http://localhost:1024

management:
  endpoints:
    web:
      exposure:
        include: '*'