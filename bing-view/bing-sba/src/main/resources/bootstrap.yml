server:
  port: 5000

spring:
  application:
    name: bing-sba
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  security:
    user:
      name: admin
      password: admin

#开启端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
